# GDI-Test
KeyEffect NameDescription1Color InvertPerforms a bitwise DSTINVERT flip across all desktop RGB values.2Screen MeltGrabs vertical screen slices and shifts them down like a liquid tunnel.3Tunnel ZoomScaled inward repeating frame effect.4Glitch ShakeRapid multi-axis screen position jitter.5Sine WaveApplies mathematical sine displacement across horizontal scanlines.6XOR Color BlastDraws randomized XOR color blocks using PATINVERT.7Block ScatterSwaps random $60\times60$ pixel regions across the screen.8Retro PixelateLowers the display buffer resolution and stretches it back out.9Spiral VortexInverted spiraling frame shrinkage sequence.0Curtain SplitSeparates and pushes the left and right desktop halves outward.-Scanline GlitchSimulates broken analog TV line noise.=Swirl PulseElastic pulsing zoom motion across the viewport.RRedraw ScreenSends an invalidate signal to Windows to immediately clean all GDI artifacts.ESCExitRestores the screen state and terminates the controller.
